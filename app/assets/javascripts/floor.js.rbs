class Floor < xeogl.Model
  def initialize(frame)
    super({})
    @frame = frame
    @floor = frame.plane_for(self)
    self.update()
  end

  def update()
    @floor.scale(@frame.width + 400 , 1, @frame.length + 400 )
    @floor.position(0, -@frame.height - @frame.size , 0)
  end
end
